"""dr_core.verify -- the D8 verification layer (citation gate, provenance audit,
claim selection, adaptive vote protocol). See dr_core.graph.verify.verify_node for
the outer-graph node that wires these together, and DECISIONS.md D8 for the ruling.
"""
